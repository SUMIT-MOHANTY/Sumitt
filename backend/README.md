# Booking System Backend API

## Admin Slots Endpoint

The `/api/admin/slots` endpoint allows administrators to create time slots for booking.

### Authentication

All admin endpoints require authentication using JWT tokens provided in the `x-auth-token` header.

### POST /api/admin/slots

Creates a new time slot that can be booked by users.

**Request Body:**
echo 'Fix completed successfully'
