/**
 * Models Index
 * Exports all models to be used throughout the application
 */

const Location = require('./Location');
const AppointmentSlot = require('./AppointmentSlot');

module.exports = {
  Location,
  AppointmentSlot
};
